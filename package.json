{
  "name": "okaycupid",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "start": "node server/index.js",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "backup": "node scripts/database.js backup",
    "restore": "node scripts/database.js restore",
    "backup:scheduled": "node scripts/database.js scheduled"
  },
  "dependencies": {
    "express": "^5.1.0",
    "openid-client": "^6.8.8",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@vitejs/plugin-react": "^5.0.0",
    "jose": "^6.2.12",
    "vite": "^7.1.0"
  }
}
